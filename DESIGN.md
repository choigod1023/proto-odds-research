# 5대 검증 과제 — 실험 설계서 (사전등록)

> 작성 2026-07-26 · 대상 레포 [`proto-odds-research`](https://github.com/choigod1023/proto-odds-research)
> 배경 문서: [README.md](README.md) — 사전조사 정리본
>
> **이 문서는 데이터를 보기 전에 작성한다.** 판정 기준을 나중에 정하면 어떤 결과든 성공으로 해석할 수 있고,
> 그게 이 분야에서 가장 비싼 실패다(README §10). 아래 임계값은 전부 **측정 착수 전에 확정**한 값이다.
> 임계값을 사후에 바꿔야 할 근거가 생기면, **바꾼 사실과 이유를 이 문서에 기록**한 뒤 바꾼다.

---

## 0. 이 설계서가 지키는 규칙

`wc2026-predictor`의 절대 원칙(CLAUDE.md)을 그대로 승계한다.

| 원칙 | 이 프로젝트에서의 적용 |
|---|---|
| 시간 순서 엄수 | 모든 검증은 walk-forward. random split 금지 |
| 데이터 누수 금지 | 모든 레코드에 **"언제 알 수 있었는가"** 타임스탬프. 배당 확정 이후 정보는 그 시점 피처로 못 씀 |
| 재현성 | 표본 추출·부트스트랩 시드 **42** 고정. 원본 응답은 gzip 캐시로 보관해 재파싱 가능 |
| 사실/의견 구분 | 산출 문서에서 **[사실]**(측정치)과 **[해석]**(추론)을 표기 분리 |
| 사전등록 | 통과/중단 조건을 측정 전에 못박는다 (본 문서) |

---

## 1. 실행 순서 — 설계 중 바뀐 점 두 가지

초판 우선순위(Q1→Q5)를 그대로 따르지 않는다. 설계하면서 두 가지가 드러났다.

### 🔑 발견 A — 수집 1회가 Q1·Q4·Q5를 전부 커버한다

`get_proto_list.htm` 응답에는 **배당과 경기 결과가 같이 들어 있다**(README §2-1). 즉:

- **Q1**(마진 재현)에 필요한 것 = 배당
- **Q4a**(캘리브레이션)에 필요한 것 = 배당 + 결과
- **Q5**(favorite-longshot)에 필요한 것 = 배당 + 결과

→ **셋 다 같은 데이터다.** 따로 수집할 이유가 없다. 한 번 긁어 로컬에 캐시하고 세 번 파싱한다.
이게 서버 부담·재현성·작업량 모두에서 유리하다.

### 🔑 발견 B — Q2는 시간이 걸리므로 **가장 먼저 인프라를 깔아야** 한다

Q2b(배당 변동 시계열)는 30분 간격으로 **2~3주를 쌓아야** 답이 나온다.
Q1·Q4·Q5는 과거 데이터라 언제 해도 결과가 같다.
→ **Q2 수집기를 제일 먼저 돌려놓고, 그게 쌓이는 동안 Q1·Q4·Q5를 처리**한다. 순차 진행하면 3주를 그냥 버린다.

### 확정 실행 순서

```
[즉시·병렬]
 ├─ S0. Q2b 스냅샷 수집기 가동 ──────────────────────┐ (2~3주 백그라운드 축적)
 ├─ S1. Q2a 아카이브 값 성격 판정 (하루 관측)        │
 └─ S2. 과거 회차 원본 일괄 캐시 (1회, 약 75분)      │
         │                                            │
         ├─→ Q1  마진 booking 상수성                  │
         ├─→ Q4a 프로토 배당 캘리브레이션 + devig 3종 │
         └─→ Q5  favorite-longshot 방향               │
                                                      │
[KBO 시즌 중이므로 즉시 가능]                          │
 └─ Q3  named vs 와이즈토토 3중 스냅샷                │
                                                      │
[데이터 축적 후] ←──────────────────────────────────┘
 └─ Q2b 결론 → 이 결과가 프로젝트 존속 여부를 결정
```

**의존성**: Q4b(해외 샤프북 devig 방법 확정)는 유료/헤드리스 데이터가 필요하므로 **Q2b 결론 이후로 미룬다.**
Q2b가 부정적이면 해외 대조 파이프라인 자체가 불필요해지기 때문이다.

---

## 2. 공통 수집 계획 (S2)

### 대상
- 연도: **2023 · 2024 · 2025** (3시즌). 2026은 3월 개편으로 제도가 달라져 별도 취급(§Q1-c)
- 회차: 각 연도 전 회차 (연 150회차 내외 추정 → 총 약 450회차)
- 종목: `sc`(축구) `bs`(야구) `bk`(농구) `vl`(배구) 4개 분할 요청 (§14-2에서 확정)

### 요청 예산
```
450 회차 × 4 종목 = 1,800 요청
요청 간격 2.5초 → 약 75분 (1회성)
```
- **원본 응답을 gzip으로 로컬 캐시** (`data/raw/wisetoto/{year}/{round}_{sports}.html.gz`)
- 이후 모든 분석은 **캐시 재파싱**. 파서를 고쳐도 재수집하지 않는다
- 캐시는 `.gitignore` 제외(용량) — 재현은 수집 스크립트로

### 예의·안전
- `User-Agent` 명시, 세션 쿠키 유지, 요청 간격 2.5초 하한
- 실패 시 지수 백오프, 3회 실패하면 해당 회차 스킵 후 로그
- 재실행 시 **캐시 존재하면 요청하지 않음**(멱등)
- 비상업 연구 목적. 수집한 원본은 재배포하지 않는다

### 파싱 검증 체크리스트 (⚠️ 통과 못 하면 모든 수치 무효)

README §14-3에서 **초판의 "배당 3,848개"가 재현되지 않았다**(재측정: `span.pt` 1,598개).
파싱 기준 자체가 검증 대상이므로, 아래를 전부 통과해야 집계로 넘어간다.

| # | 검증 | 실패 시 |
|---|---|---|
| P1 | 마켓 유형별 배당 개수가 정확히 2 또는 3개인가 | 셀렉터 재설계 |
| P2 | **무효/취소 경기(배당 1.0) 제외**했는가 | 오버라운드 전체 오염 — 반드시 제외 |
| P3 | 오버라운드가 [1.00, 1.40] 범위 안인가 | 범위 밖은 파싱 실패로 간주하고 로그 |
| P4 | 게임행 수 = 회차 공시 게임 수와 일치하는가 | 누락 행 존재 |
| P5 | 결과 라벨(홈승/무/원정승/오버/언더)이 스코어와 정합인가 | 결과 파싱 오류 |
| P6 | 초판 표(§1)의 99회차 수치가 **재현되는가** | ⭐ 재현 실패 시 초판 수치를 폐기하고 신규 측정으로 대체 |

---

## Q1. 마진 booking은 상수인가

### 검증 명제
> 프로토는 경기별로 배당을 산정하지 않고, **마켓 유형별 목표 환급률을 정해 기계적으로 산출**한다.

초판 근거는 2025년 99회차 **단일 회차**의 회차 *내부* 표준편차 0.0007이다.
이건 "회차 안에서 균일하다"는 뜻일 뿐, **회차 간·연도 간에도 같은 값인지는 측정된 바 없다.**

### 세부 질문과 사전등록 판정 기준

| ID | 질문 | 판정 기준 (사전 확정) |
|---|---|---|
| **Q1-a** | 유형별 booking이 **회차 간** 상수인가 | 회차별 평균 booking의 SD **< 0.002** → 상수로 간주<br>(회차 내 SD 0.0007의 3배를 허용폭으로) |
| **Q1-b** | **연도 간** 변하는가 | 연도별 평균 차이 **≥ 0.005**(환급률 0.4%p) → "시기별 변동 있음" → devig 시 **연도별 booking을 써야 함** |
| **Q1-c** | **2026-03-27 개편**으로 바뀌었는가 | 개편 전후 평균 차이 ≥ 0.005 → 개편 후 데이터만 실전 기준선으로 사용 |
| **Q1-d** | **종목별로** 다른가 | 종목 간 최대 차이 ≥ 0.005 → 종목별 booking 분리 적용 |
| **Q1-e** | **야구 단독 회차 vs 혼합 회차** (§11-7) | 차이 ≥ 0.005 → 회차 구성이 booking에 영향 |
| **Q1-f** ⭐ | **배당 수준에 따라 마진 배분이 다른가** | `overround ~ β·(1/o_favorite)` 회귀에서<br>β가 p<0.01로 유의 **AND** 예측 범위 폭 > 0.003<br>→ **균등 마진 가정 기각** |

### Q1-f를 추가한 이유 [해석]
실제 북메이커는 마진을 균등 배분하지 않는다. 역배 쪽에 마진을 더 얹는 것이 favorite-longshot bias의 공급측 원인이라는 것이 표준 설명이다.
**만약 프로토도 그렇다면 multiplicative devig는 구조적으로 틀리고, power/shin이 선택이 아니라 필수가 된다.**
→ Q1-f 결과가 **Q4의 방법 선택과 Q5의 편향 해석을 동시에 좌우**한다. 초판 설계엔 없었으나 가장 중요한 항목일 수 있다.

### 산출물
- `data/processed/q1_booking.csv` — (연도, 회차, 종목, 마켓유형, 게임수, 평균 오버라운드, SD)
- `findings/Q1.md` — 유형×연도×종목 매트릭스, Q1-f 회귀 결과

### 편향·위험
- ⚠️ 아카이브 값이 **마감 배당일 가능성**(Q2a 미해결) → 본 결과는 **"마감 시점 booking"으로 한정 해석**. Q2a 결론이 나오면 이 절의 해석 문구를 갱신한다
- ⚠️ 무효 경기 배당 1.0 미제외 시 오버라운드가 붕괴 → 체크리스트 P2

---

## Q2. 정보 시차는 실재하는가 ⭐ 프로젝트 존속 조건

### 검증 명제
> 프로토 배당은 발매 개시 시점에 고정되고, 경기 전까지 **유의미하게 갱신되지 않는다.**
> 따라서 발매 후 발생한 정보(선발 변경·부상·날씨)는 배당에 반영되지 않은 채 남는다.

README 전체 전략이 이 명제 위에 서 있다. 그런데 §4는 **"발매 중 배당 변경 가능"**이라고 적고 있다.
즉 **명제와 제도 설명이 이미 충돌**하며, 아직 아무도 실측하지 않았다.

### Q2a — 아카이브 값의 성격 (즉시, 하루)

**질문**: 와이즈토토 아카이브가 보관하는 배당은 **최초 공개값인가 마감값인가** (§11-2)

**방법**: 현재 발매 중인(마감 전) 회차를 **동일 URL로 4시간 간격 반복 조회**하고 값 변화를 관찰.

| 관측 | 결론 |
|---|---|
| 값이 변한다 | 아카이브는 **현재값을 반영** → 최종 보관값 = 마감 배당 → 과거 회차로는 변동 이력 복원 불가 |
| 값이 고정 | 아카이브는 **최초 공개값** 보관 → 과거 데이터로도 "최초값" 확보 가능 |

**중요**: 어느 쪽이든 **Q2b(실시간 스냅샷)는 필요하다.** 전자면 변동 이력이 실시간 수집으로만 얻어지고, 후자면 마감값이 실시간 수집으로만 얻어지기 때문이다.

### Q2b — 배당 변동 시계열 (2~3주 축적)

**수집**: 발매 중 회차 전 경기를 **30분 간격** 스냅샷 → `data/raw/snapshots/{ts}/...`

**측정 지표**
1. **변동 발생률** — 발매~마감 사이 배당이 1회 이상 바뀐 경기 비율
2. **변동 크기** — |Δ내재확률|의 분포 (중앙값·90분위)
3. **변동 타이밍** — 킥오프 대비 언제 움직이는가 (히스토그램)
4. ⭐ **해외 배당과의 동조성** — 같은 구간 해외 컨센서스 변화량과의 상관 `corr(Δp_proto, Δp_sharp)`

### 사전등록 판정 기준

| 결과 | 판정 |
|---|---|
| 변동률 **< 20%** AND 변동이 킥오프 24시간 전에 정지 | ✅ **통과** — 시차 델타 존재. L3 피처(선발·불펜·날씨) 투입 정당화됨 |
| 변동률 20~50% | 🟡 **조건부** — 변동이 없는 경기만 대상으로 전략 축소 |
| `corr(Δp_proto, Δp_sharp)` **> 0.7** | 🛑 **중단** — 프로토가 해외를 실시간 추종. 시차 델타 전제 폐기 |
| 변동이 킥오프 직전까지 지속 | 🛑 **중단** — 정보가 이미 반영됨 |

🛑 **중단 시 대응**: 전략을 폐기하되 프로젝트는 **"한국 고정배당 시장은 효율적이다"를 실증한 연구**로 전환한다.
README §12가 이미 예고한 대로, **결과가 어느 쪽이든 공개 연구로서의 가치는 유지**된다. 이 갈림길을 미리 인정해 두는 것이 사전등록의 핵심이다.

### 산출물
- `data/raw/snapshots/` — 원본 스냅샷
- `data/processed/q2_movements.csv` — 경기별 배당 변동 이력
- `findings/Q2.md`

---

## Q3. named `domestic`은 무엇인가

### 검증 명제
> named API의 `representativeOdds.domestic`은 프로토 원본 배당이다.

초판 측정이 이미 이걸 반증했다: **오버라운드 1.1111 vs 1.1362**.

### ⭐ 착수 전 관찰 [해석]
**1.1111 = 10/9 에 정확히 일치한다.** 환급률로는 정확히 90.00%.
실측 배당의 합이 이렇게 딱 떨어지는 일은 드물다 → **인위적 정규화의 흔적**으로 보인다.
이 관찰은 아래 H2(가공값)를 강하게 시사하며, 검증 순서를 H2부터로 잡는 근거다.

### 경쟁 가설

| ID | 가설 | 기각 방법 |
|---|---|---|
| **H2** | named가 배당을 **90% 환급률로 재정규화한 가공값** | 프로토 배당을 90%로 재정규화했을 때 named와 일치하는가 |
| **H1** | **다른 상품**(토토 승부식 vs 프로토, 다른 마켓) | 확률 **순위·비율**이 프로토와 다른가 |
| **H3** | **시점차** (다른 시각 스냅샷) | 시각을 맞춰 동시 수집해도 차이가 남는가 |
| **H4** | 초판 **측정 오류** | 월드컵 3-way 축구의 프로토 booking은 1.1494인데 1.1111은 그 어느 것도 아님 → 재측정으로 확인 |

### 방법
KBO 시즌 중(2026-07 현재)이므로 즉시 가능.
**같은 경기·같은 시각**에 3중 스냅샷을 저장한다: `named.domestic` / 와이즈토토 프로토 원본 / 해외 컨센서스.
표본 **30~50경기**.

### 사전등록 판정 기준
- `corr(p_named, p_proto) > 0.99` **AND** 재정규화 후 평균절대차 **< 0.005** → **H2 확정**(가공값). named는 프로토의 변환이므로 **대조용으로만 사용 가능, 기준선 불가**
- 상관이 0.99 미만이거나 순위가 뒤바뀜 → **H1**(다른 상품). named는 폐기
- 시각 정렬 후 차이 소멸 → **H3**

### 겸사겸사 측정 (같은 수집으로 가능)
> README §5는 **"프로토 배당이 해외 배당을 참조해 만들어진다"**를 전제로 깔았지만 이 역시 근거가 없다.

같은 3중 스냅샷으로 **해외 오프닝 시점 vs 프로토 최초 공개 시점의 시차·상관**을 잰다.
- 상관 높음 + 프로토가 뒤 → 해외 참조 + 마크업 구조 → 시차 델타는 **해외 갱신 이후 구간**에 존재
- 상관 낮음 → 국내 자체 산정 → 델타 구조가 완전히 다름

→ 이 결과는 **Q2b의 해석 틀을 결정**한다.

### 산출물
`data/processed/q3_triple_snapshot.csv`, `findings/Q3.md`

---

## Q4. devig — 어떤 방법이 프로토 배당을 실제 확률로 잘 사상하는가

### ⚠️ 질문을 정확히 다시 씀
"어떤 devig가 옳은가"는 **답할 수 없는 질문**이다. 진짜 fair probability는 관측 불가능하기 때문이다.
답할 수 있는 형태로 바꾼다:

> **어떤 devig 방법이 프로토 배당을 실제 결과 빈도에 가장 가깝게 사상하는가.**

이건 실용적으로 우리가 필요한 바로 그 값이다(EV 계산의 분모).

### Q4a — 프로토 배당 캘리브레이션 (S2 캐시로 즉시)

**데이터**: Q1과 동일한 수집물. 추가 수집 없음.

**절차**
1. 3종 devig 구현
   - **Multiplicative**: `p_i = (1/o_i) / Σ(1/o_j)`
   - **Power**: `p_i ∝ (1/o_i)^k`, `Σp=1` 되도록 k를 수치해
   - **Shin**: 내부자 비율 z 가정, 반복 알고리즘
   - (**Additive**: 초과분 균등 차감 — 비교 기준용)
2. 각 방법으로 프로토 배당 → 확률
3. 확률 10분위 구간별 **예측확률 vs 실제 적중률** (신뢰도 곡선)
4. **Brier · log loss · ECE**(expected calibration error) 산출
5. 시간 분할 walk-forward: 2023~2024 학습(Shin의 z, Power의 k 추정) → 2025 검증

**사전등록 판정**: 2025 검증 구간에서 **ECE가 가장 낮은 방법**을 채택.
동률(차이 < 0.002)이면 **더 단순한 쪽**(multiplicative)을 택한다.

**기준선**: 어떤 방법도 base-rate 예측보다 ECE가 낮지 않으면, 프로토 배당은 정보가 없다는 뜻 → 별도 보고

### Q4b — 해외 샤프북 devig (Q2b 결론 이후로 연기)
Q2b가 🛑이면 해외 대조 파이프라인 자체가 불필요해진다. **선행 투자하지 않는다.**

### 산출물
`src/devig.py`, `data/processed/q4_calibration.csv`, `findings/Q4.md` (신뢰도 곡선 포함)

---

## Q5. 한국판 favorite-longshot 편향의 방향

### 검증 명제
> 한국 프로토 KBO 시장에서도 **역배가 과대평가**되어 있다 (Snowberg & Wolfers와 같은 방향).

**왜 중요한가**: 방향이 반대면 전략이 정반대가 된다.
- 미국식 FLB → 저평가된 쪽은 **강팀** → README §12의 "역배 맞히기 금지" 규율이 유효
- 역FLB(한국 특이) → **역배 쪽**이 기회 → 그 규율을 뒤집어야 함

인기구단(두산·기아·롯데·LG·삼성) 쏠림이 크다면 후자가 나올 수 있다. **가정하지 말고 측정한다.**

### 데이터
Q1·Q4와 동일 수집물. KBO 2023~2025. 추정 표본 **연 720경기 × 3 ≈ 2,000경기**(프로토 발매분 한정이라 실제로는 더 적음).

### 절차
1. 배당 구간별 **균등 베팅 ROI** — 구간: `1.2–1.5 / 1.5–1.8 / 1.8–2.2 / 2.2–3.0 / 3.0+`
2. 회귀 `ROI ~ β·log(odds)` — **β의 부호가 판정**
3. **인기구단 더미** 회귀 — 전통 인기구단 베팅의 ROI가 유의하게 낮은가
4. ⭐ **부트스트랩 신뢰구간**(시드 42, 10,000회) — ROI는 분산이 매우 커서 점추정만으론 아무 말도 못 한다

### 사전등록 판정 기준

| 결과 | 판정 |
|---|---|
| β **유의 음수** (p<0.05, 부트스트랩 CI가 0 미포함) | 미국식 FLB → 전략 방향 = **강팀** |
| β **유의 양수** | 역FLB(한국 특이) → 전략 방향 = **역배** ← 발견 시 그 자체로 논문거리 |
| CI가 0을 포함 | **방향성 없음** → 배당대 필터는 쓸모없음. 다른 필터 필요 |

⚠️ **표본 경고**: 구간당 수백 경기면 ROI 표준오차가 ±5%p를 넘기 쉽다.
**"ROI가 +3%p 나왔다"는 것만으로 편향을 주장하지 않는다.** CI가 판정하고, 점추정은 보고만 한다.

### 산출물
`data/processed/q5_flb.csv`, `findings/Q5.md`

---

## 3. 진행 현황

| 단계 | 상태 | 갱신 |
|---|---|---|
| 설계서 확정 | ✅ 완료 | 2026-07-26 |
| S0 Q2b 수집기 가동 | ⚪ 대기 | |
| S1 Q2a 아카이브 성격 판정 | ⚪ 대기 | |
| S2 과거 회차 캐시 | ⚪ 대기 | |
| Q1 | ⚪ 대기 | |
| Q3 | ⚪ 대기 | |
| Q4a | ⚪ 대기 | |
| Q5 | ⚪ 대기 | |
| Q2b 결론 | ⚪ 대기 | |

---

## 4. 설계 변경 이력

| 일자 | 변경 | 이유 |
|---|---|---|
| 2026-07-26 | 최초 작성 | — |
| 2026-07-26 | Q1에 **Q1-f(배당 수준별 마진 배분)** 추가 | 균등 마진 가정이 깨지면 Q4·Q5 해석이 통째로 달라짐 |
| 2026-07-26 | Q2를 **최우선 착수**로 이동 | 축적에 2~3주 필요 — 순차 진행 시 그 기간을 낭비 |
| 2026-07-26 | Q4를 **Q4a(즉시) / Q4b(연기)**로 분리 | Q4a는 S2 캐시로 추가 수집 없이 가능. Q4b는 Q2b 결과에 종속 |
| 2026-07-26 | Q3에 **해외-프로토 시차·상관 측정** 추가 | README §5의 "프로토는 해외를 참조한다" 전제도 미검증 |
