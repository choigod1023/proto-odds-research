# 인수인계 — 여기까지 무엇을 했고, 다음에 뭘 해야 하나

작성 2026-07-28. 이어받는 사람이 **처음부터 다시 파지 않도록** 쓴 문서다.
막다른 길도 전부 적어 뒀다. 그게 이 문서의 절반이다.

---

## 0. 목표 (흐리면 안 되는 것)

**베팅 시장보다 나은 확률을 잡아 적중시키는 것.** 학술 호기심이 아니라 돈이다.

대상은 한국 프로토(고정배당). 사용자는 한국 거주자이므로 **해외 베팅 사이트는
국민체육진흥법 위반**이다. 합법 경로는 오프라인 판매점과 betman.co.kr 뿐이고,
해외 배당은 **참고 신호로만** 쓴다. 베팅 사이트 연동·자동 베팅은 만들지 않는다.

### ⚠️ 넘어야 할 문턱

프로토 환급률은 **약 88%** 다. 즉 **시장과 비기면 12% 를 잃는다.**
"시장만큼 정확하다"는 목표가 아니다. **마진을 넘길 만큼 앞서야** 한다.
이 문턱을 잊으면 모든 결과가 실제보다 좋아 보인다.

---

## 1. 지금까지 확인된 지형 (한 장 요약)

| 질문 | 답 | 근거 |
|---|---|---|
| 배당 구조만으로 +EV 구간이 있나 | ❌ 134개 슬라이스 중 0개 | `Q0.md` |
| 팀 단위 변수로 시장을 이기나 | ❌ 시장이 이미 반영 | `변수분석.md` |
| 모델 정교화(pi-ratings)로 되나 | ❌ 격차의 15% 만 메움 | `변수분석.md` |
| 선발 정보 시차를 노릴 수 있나 | ❌ 3개 리그에서 재현 안 됨 | `정보시차_선발.md` |
| 축구 라인업 정보는 | ❌ 표본 부족, 판정 불가 | `라인업.md` |
| 해외 배당 괴리는 | ❌ +0.9%, 그마저 마감배당(미래정보) | `해외배당대조.md` |
| **선발투수 지표는** | ✅ **+0.006, 박빙에선 +0.014** | `박빙과xFIP.md` |
| **박빙에서 시장을 이기나** | ⚠️ **못 이긴다. 다만 격차가 사라진다** | `박빙시장대조.md` |
| 축구 xG 는 쓸모 있나 | ⚠️ 단독=무승부, 조합하면 +0.004 | `xG관문.md` |
| 타선 지표는 | ❌ **통째로 죽음** | `타선무용.md` |
| 구장 파크팩터는 | ❌ **신호는 진짜인데 시장이 이미 안다** | `파크팩터.md` |

**요약: 시장 우위가 지워진 구간은 딱 하나 — 박빙(시장 45~55%)이다.**
그리고 거기서도 이기는 게 아니라 비긴다. 88% 문턱을 못 넘는다.

---

## 2. ⭐ 가장 중요한 것 — 가설이 한 번 바뀌었다

### 틀렸던 가설
> "결과 지표(득점·실점)보다 **과정 지표**(FIP·xG·wOBA)가 낫다."

야구 투수에서 잘 맞았다. ERA(+0.00257) → FIP(+0.00432) → xFIP(+0.00597).

**그런데 타선에서 완전히 깨졌다.** 타선의 과정 지표(wOBA·K%·BB%·ISO)가
전부 z<1.6, 개선 +0.0002 수준으로 죽었다. Elo 를 빼고 재도 마찬가지였다
(겹침 문제가 아니라 신호 자체가 없다).

### 수정된 가설 (현재 유효)
> **경기마다 바뀌는 정보인가, 아니면 팀 레이팅이 이미 아는 것인가.**

| | 경기 간 변동 | 효과 |
|---|---|---|
| 선발투수 | 매 경기 다른 사람. 에이스와 5선발은 딴판 | **+0.006** |
| 타선 | 매일 같은 라인업. 팀 수준 주변 진동 | **≈0** |

FIP > ERA 였던 것도 "과정이라서"가 아니라 **선발이라는 올바른 대상을 잡은 뒤
측정 정밀도를 올린 것**으로 읽어야 한다.

축구 xG 도 이 틀에 맞는다 — xG 가 득실차를 밀어냈지만(회귀에서 득실차 z 가
7.05→1.95 로 붕괴) 얻은 건 +0.004 로 작았다. **둘 다 팀 단위 정보라,
새 정보가 아니라 같은 걸 더 정확히 잰 것**이기 때문이다.

**→ 앞으로 피처를 고를 때 이 기준으로 거른다.**

### 🔄 2026-07-28 재수정 — 조건이 하나 더 있다

파크팩터가 위 기준을 **둘 다 만족하는데도 실패했다**(`파크팩터.md`).
walk-forward 로 잰 구장 효과는 진짜였고(총득점 RMSE 4.7578→4.7297, 우위확률
97.9%) 구장 순서도 상식과 맞았다(대구 1.061 · 잠실 0.945 · 고척 0.913).
그런데 **시장 확률 위에 얹으니 오히려 나빠졌다**(Brier +0.0022, 우위확률 4.0%).

> ① 경기마다 바뀌는가  ② Elo 가 모르는가  **③ 시장도 모르는가**

**Elo 가 모르는 것 ≠ 시장이 모르는 것.** 시장은 Elo 보다 훨씬 많이 안다.
구장은 일정만 보면 알 수 있고 몇 년째 그대로라, 북메이커가 놓칠 리 없다.

③을 만족하려면 **"프로토 배당이 굳은 뒤에 알려지는 것"**이어야 한다.

⚠️ **순서를 바꿀 것.** 지금까지 관문1(모델 내부) → 관문2(대 시장) 순으로 쟀는데,
**관문3(시장 위에 얹기)이 가장 싸고 가장 잘 거른다.** 새 피처는 이걸 먼저 돌려라.

---

## 3. 다음에 할 일 (우선순위)

### ~~1순위 — 구장 파크팩터~~ ✅ 완료, 죽었다 (2026-07-28)
`파크팩터.md` 참조. 신호는 진짜였으나 **시장이 이미 반영**해 관문 3 탈락.
`src/park_factor.py` 는 관문1→2→3 을 한 번에 도는 하네스라 **다음 피처 검증에
그대로 재사용**할 수 있다(`load_ou()`·`boot_diff()`·관문3 블록).

### 1순위 — 심판(주심) ⭐ 유일하게 ③을 만족할 가능성이 있는 후보
경기마다 바뀌고, Elo 가 모르고, **배정이 늦게 공개되면 시장도 모른다.**
- 확인 필요: 네이버 `record` 응답의 `etcRecords` 에 주심 이름이 있는가
- 있으면 주심별 walk-forward 스트라이크존 성향(볼넷·삼진·총득점) 산출
- ⚠️ **관문 3(시장 위에 얹기) 부터 돌릴 것.** 여기서 죽으면 나머지는 볼 필요 없다
- ⚠️ 주심 배정이 **경기 당일 공개**인지 확인이 선행돼야 한다. 사전 공개면 ③ 탈락

### 2순위 — 날씨 (단, '갱신되는 부분'만)
기온·바람은 구장처럼 예보로 미리 알 수 있어 시장이 반영했을 것이다.
쓸모가 있다면 **프로토 배당이 굳은 뒤 예보가 바뀐 경우**뿐이다.
→ 배당 확정 시점 예보와 경기 직전 예보를 **둘 다** 저장해야 측정이 된다. 미수집.

### 죽은 후보 (다시 하지 말 것)
- 구장 파크팩터 — 시장이 안다(`파크팩터.md`)
- 야간/주간·요일 — 일정에 있으므로 시장이 안다. ③ 탈락이 명백
- 불펜 가용성 — z=0.51 로 죽었다

### 3순위 — 박빙 구간을 마진 너머로
`박빙시장대조.md` 가 유일한 승산 방향이다. 여기에 위 피처들을 쌓아
시장 대비 격차를 **환급률 12% 를 넘길 만큼** 벌려야 한다.
현재 박빙 검증 표본이 307경기라 ±0.007 로 흔들린다 — 시즌이 쌓여야 판정력이 는다.

### 4순위 — K리그 현재 시즌 xG
`xG관문.md` 가 xG 의 값어치(+0.004)를 확인했으므로 비용 근거가 생겼다.
단, **한 방이 아니라 쌓을 재료**라는 걸 기억할 것.

---

## 4. 인프라 (지금 돌고 있는 것)

### fly.io 상시 수집 — `proto-odds-collector`
노트북을 덮어도 안 끊긴다. 자세한 운영은 `deploy/README.md`.

| 수집기 | 주기 | 대상 |
|---|---|---|
| `snapshot.py` | 15분 | 와이즈토토 프로토 배당 |
| `info_watch.py` | 30분 | 네이버 KBO/MLB/NPB 선발 예고 |
| `overseas_watch.py` | 15분 | BetExplorer 해외 배당 |
| `xg_watch.py` | 하루 1회 | FootyStats K1·K2·J1·J2 팀 xG |

30분마다 `data/` 만 커밋해 GitHub push → 웹(Vercel) 자동 재배포.
코드는 이미지에 굽지 않고 볼륨의 git clone 에서 돈다
→ **수정 반영은 `git push` + `fly machine restart`**.

⚠️ **push 하지 않은 로컬 커밋은 서버에 없다.** 이 함정에 한 번 빠졌다.
⚠️ 로컬 수집기는 꺼 뒀다. 동시에 돌리면 `data/` 에서 git 충돌이 난다.

비용 월 $3.7 안팎. `fly apps destroy proto-odds-collector` 로 정리.

### 레포
- 모델·연구: `github.com/choigod1023/proto-odds-research` (이 레포)
- 웹: `~/proto-odds-web` (Next.js 15, **아직 GitHub 미푸시·미배포**)
- 월드컵 예측(별개 프로젝트): `github.com/choigod1023/wc2026-predictor` + `wc2026-web`

---

## 5. ⚠️ 데이터 접근 정책 — 지켜온 선

**robots.txt 가 막은 경로는 자동 수집하지 않는다.** 봇 차단 우회 요청도 거절했다.
데이터가 있는데 못 쓰는 곳이 셋이다:

| 소스 | 갖고 있는 것 | 막힌 이유 |
|---|---|---|
| FootyStats | K리그 경기별 xG CSV | `Disallow: /c-dl.php*` + 로그인 필수 + K리그는 유료 |
| FotMob | 경기별 xG·npxG·xGOT | `Disallow: /api/*` |
| Understat | 유럽 경기별 xG 전 시즌 | **`Disallow: /`** (사이트 전체) |

**사람이 브라우저로 직접 받는 건 다른 얘기다** — 사이트가 제공하는 기능을
사용자가 쓰는 것이므로 정상 경로다. FootyStats 유료 계정을 사면 K리그 경기별
CSV 를 받을 수 있다. 돈 쓰는 결정이라 대신 하지 않았다.

허용된 경로로 쓰는 것:
- FootyStats **페이지**: 팀 시즌누적 xG → `xg_watch.py` 가 전진 스냅샷
- FotMob **경기 페이지**(`__NEXT_DATA__`): 경기별 xG. 단 **과거는 못 쓴다**(아래)
- StatsBomb 오픈데이터: 명시적 무료 공개. 1,517경기 확보 완료

---

## 6. 🕳 함정 모음 (같은 데 두 번 빠지지 말 것)

### 데이터
1. **프로토는 같은 경기를 여러 회차에 중복 발매한다.** `matches.py` 가 (리그,홈,원정,날짜)로
   중복 제거하고 **날짜순** 정렬한다. 안 하면 한 팀 시즌이 1.4배로 부풀고
   회차 순서는 시간 순서가 아니라 '최근 10경기'가 무의미해진다.
2. **언더오버 배당 순서는 `[언더, 오버]`** 다. 뒤집었더니 양쪽 다 +ROI 라는
   불가능한 결과가 나왔다(26% 오염, 가짜 ROI +54%). `verify_mapping.py` 로 검증.
3. **3-way 를 2-way 로직으로 계산하면 ROI +64%** 라는 가짜가 나온다.
   무승부를 원정승에 합산하는 실수였다.
4. **FotMob URL 은 경기가 아니라 팀 조합을 가리킨다.** 포항vs대전 4경기가 전부
   같은 해시. 경기 구분은 `#앵커`뿐인데 프래그먼트는 서버로 안 간다
   → 120경기 요청에 32경기가 2번씩 = 64레코드였다. **과거 이력에 쓸 수 없다.**
5. **네이버 타격 이닝 표기**: 방향이 2글자인 경우가 있고(`우중2`) 한 칸에
   타석 여럿이 `/` 로 들어간다(`좌중안/2땅`). 접두사로 잡으면 2루타 40% 유실.
   **접미사 판정 + `/` 분할**이 정답. 검산 2,896경기 100% 일치.

5-b. **games.csv 의 팀명 표기가 마켓마다 다르다.**
   승패는 `home="두산 12"` / `away="10 롯데"` (스코어 포함), 언더오버는
   `home="두산"` / `away="롯데"` (팀명만)이다. `matches._home()` 은 앞 형식만
   파싱하므로 언더오버에 그대로 쓰면 **조인이 통째로 0건**이 된다.
5-c. **pandas 3.0 의 Arrow 문자열 컬럼에 `.map()` 을 쓰면 반환 dtype 이 str 로
   유지된다.** 이후 `groupby().median()` 이 `TypeError` 로 죽는다. 리스트
   컴프리헨션 + `dtype="float64"` 명시로 피한다.

### 방법론
6. **점추정만 보고 판단하지 말 것.** 박빙 대조에서 −0.00029 를 '모델 우위'로
   읽을 뻔했다. 부트스트랩하니 우위확률 53.7% = 동전 던지기였다.
   그리고 밴드를 좁히니 오히려 나빠져 노이즈임이 확정됐다.
7. **표본이 늘면 부호가 뒤집힐 수 있다.** xG vs 득실차는 검증 196경기에서
   득실차 우위, 382경기에서 xG 우위였다. 둘 다 CI 가 0 을 포함했다.
8. **필요 표본은 계산할 수 있다.** n=307 에서 CI 반폭 0.0072 였으므로
   **필요 n = (0.126/효과크기)²**. 효과 +0.006 이면 441경기.
9. **EV 최대화는 모델이 가장 크게 틀린 경기를 고른다**(역선택). 모델<시장일 때
   격차 상한을 두지 않으면 −23%, 상한 0.02 면 −3.5% 였다.
10. **리그를 섞으면 리그 효과를 넣어야 한다.** 리그마다 득점 수준이 다르다.
    표본만 합치고 빼먹으면 없는 신호가 생긴다.
11. **노이즈 변수는 통제변수로 써도 해롭다.** rest_diff 를 통제로 넣으니
    기준선 RMSE 가 1.449→1.510 으로 나빠졌다.
12. **인과를 거꾸로 읽지 말 것.** 선발 '평균 이닝'이 강한 걸 "불펜 부담"으로
    해석했는데, 불펜을 직접 재니 z=0.51 로 죽었다. 좋은 투수라서 길게 던지는
    것이지 길게 던져서 이기는 게 아니었다.

### 운영
13. `fly machine restart` 는 수집기를 중간에 죽여 **락이 남는다**. supervisor 가
    부팅 시 `data/raw/*.lock` 을 지우게 해 뒀다.
14. FootyStats 는 **버스트를 막는다**. robots 의 Crawl-delay 는 1초지만 그
    속도로 66페이지를 훑으면 429 다. 편성 경기만(향후 14일) 긁도록 바꿔
    요청 6배 절감 + 실패 56→0.
15. **football-data.co.uk 는 이 환경에서 연결 자체가 차단**된다(robots.txt 조차
    reset). 과거 배당이 필요하면 다른 경로를 찾아야 한다.

---

## 7. 파일 지도

### 핵심 파이프라인
```
wisetoto.py     프로토 아카이브 파서 (DOM: div.gameinfo ul / li.a3.{sc|bs|bk|vl})
build_dataset.py → data/processed/{games,bets}.csv
matches.py      ⭐ 중복 제거 + 날짜 정렬. 여기가 가장 틀리기 쉽다
features.py     Elo·폼·휴식 등 경기 전 피처
devig.py        multiplicative / additive / power / shin
score_dist.py   결합 포아송 + Dixon-Coles → 모든 마켓 확률
generate_v2.py  전 마켓 픽 생성 (경기 단위 묶음, 격차 상한 0.02)
```

### 검증 실험 (재현 가능)
```
q0_scan.py         배당 구조만으로 +EV 슬라이스가 있나 → 없다
variable_impact.py 팀 단위 변수 영향
pitcher_fip.py     ERA vs FIP
pitcher_xfip.py    xFIP · 불펜 · 박빙 구간
close_market.py    ⭐ 박빙 구간 시장 대조 (부트스트랩 포함)
batter_process.py  타선 과정 지표 → 전부 죽음
statsbomb_xg.py    StatsBomb 오픈데이터 → 경기별 xG
xg_vs_goals.py     ⭐ xG vs 득실차 관문 (단독/조합/부트스트랩)
soccer_process.py  유효슈팅 (xG 대용품) → 부족함이 확인됨
park_factor.py     ⭐ 구장 파크팩터 → 관문1 통과·2 탈락·3 탈락(시장이 안다)
                      **관문1→2→3 하네스라 다음 피처에 그대로 재사용 가능**
```

### 수집기 (fly.io 에서 상시)
```
snapshot.py       프로토 배당 시계열
info_watch.py     선발 예고
overseas_watch.py BetExplorer
xg_watch.py       FootyStats 팀 xG (전진 스냅샷)
game_detail.py    네이버 경기 상세 (baseball/batters/lineup/shots)
```

---

## 8. 사용자에 대해 기억할 것

- 학부생이다. **용어와 의도를 먼저 설명하고 착수할 것.** 혼자 진행하면 화낸다.
- **돈을 벌고 싶다.** "공개 연구 포트폴리오" 같은 프레이밍을 싫어한다.
- 시장 해설 말고 **픽**을 원한다. 한 줄 통계가 아니라 **픽스터처럼** 근거를 서술.
- 모델 변경은 **논문 근거 + 백테스트** 필수. UI 는 탈-AI 스타일 선호.
- 처음부터 **KBO·K리그**가 목표다. J리그도 할 예정.
- 어려운 용어(EV, 이상치 의심 등)를 화면에 그대로 노출하지 말 것.

---

## 9. 한 문장 요약

시장을 이긴 구간은 아직 없다. **박빙에서 유일하게 비겼고**, 그 이유는 팀
레이팅이 무력한 구간을 **선발투수라는 그날의 정보**가 메웠기 때문이다.

파크팩터로 배운 것: **Elo 가 모른다고 시장도 모르는 게 아니다.** 구장 효과는
진짜였지만 시장이 이미 값을 매겨 놨다. 그래서 심사 기준이 하나 늘었다 —
**경기마다 바뀌고 · Elo 가 모르고 · 시장도 모르는 것.** 세 번째를 만족하려면
사실상 **프로토 배당이 굳은 뒤에 알려지는 정보**여야 한다.

다음 후보는 **주심**이다. 배정 공개 시점이 배당 확정보다 늦다면 ③을 만족한다.
그것부터 확인하고, 관문 3 을 먼저 돌려라.
