# 반복 실행 인계 — 첫 두 후보 완료

## 실제 완료한 실험

1. K1 고정 ridge: 421경기 학습, 338경기 평가. 단순 최근 평균은 앞섰지만
   상대 포함 평균 대비 확실한 우위 없음. 2026년에는 악화. **승패 모델 확대 보류**.
   `soccer-verified.md`가 최종 코드의 재현 결과다.
2. MLB 조건부 타석 부분 풀링: 원본 30경기/3일, 수용 2,232타석.
   첫날을 제외한 15경기/2일/1,172타석 평가에서 로그손실 1.328544 → 1.328277,
   Brier 0.687013 → 0.687040. **표본 부족 및 혼합 결과**.
   `../mlb_pa_gate/report-20260909.md`와 `../mlb_pa_gate/README.md` 참조.

이 결과는 승패 적중률 개선치가 아니다. 운영 예측은 변경하지 않았다.
리그별 독립 실험을 합쳐 하나의 적중률로 표시하지 않는다.

## 다음 실행 우선순위

1. 이 폴더 README의 사전 고정한 K1 동적 공격·수비 count 모델을 독립 코드로 구현하고
   같은 두 기준과 비교한다. 기존 ridge의 파라미터를 반복 조정하는 것은 다음 가설이 아니다.
2. MLB는 적어도 여러 달의 선수 과거 이력과 여러 평가 날짜가 필요하다.
   기존 로컬 캐시의 추가 가용성을 우선 확인하고, 없다면 무료 공식 소스에서
   데이터 크기·요청 수를 먼저 산정해 한 실행당 최대 50요청·100MB로 제한한다.
   403/429 응답이면 중단하고 우회하거나 재시도 폭주하지 않는다.
   타석 분류의 실책/포수방해 제외와 박스스코어 타석 수 대조를 먼저 보완한다.
3. 중간 예측 개선이 재현된 뒤에만 시점이 증명되는 배당·라인업으로 실제 추천 정책을
   평가한다. 과거 결과를 추가로 본 것은 새로운 독립 표본이 아니다.

## 중복 방지 및 원장

- 축구: 현재 작업트리 `outputs/process-gate/research.sqlite3`의 `runs`.
  입력+코드+설정 해시가 같은 실행은 차단한다. 첫 실행과 핸들 종료 수정 후 재실행은
  한 가설의 기술적 재현이며, 독립 성공 횟수를 늘리지 않는다.
- MLB: 병렬 작업트리 `../mlb-pa-gate-20260909/experiments/mlb_pa_gate/private/run-20260909/results.sqlite`.
  중앙 원장의 `external_runs`에 경로/파일 해시/코드 해시/요약을 연결해 보존한다.
- MLB 코드 SHA256: `83d9c5eb167805490ab4b64808bc1e5ec72d96c9f916ca2e7dff25f4ed97b36c`.
- 원본·개별 예측 SQLite는 private이며 PR에는 코드·테스트·집계 Markdown만 들어간다.
- 새 실행은 이 기록과 대화의 후속 결과를 먼저 읽는다. 같은 데이터로 같은 모델을
  다시 돌리는 것을 다음 실험으로 세지 않는다. 별도 경로 출력 기능은 재현용이다.
